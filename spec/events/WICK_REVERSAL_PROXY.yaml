version: 1
kind: event
event_type: WICK_REVERSAL_PROXY
maturity: standard
synthetic_coverage: covered
active: true
status: validated
description: "Compatibility wick-reversal surface using hybrid evidence: wick and reclaim structure confirmed by realized-volume expansion."
reports_dir: liquidity_dislocation
events_file: liquidity_dislocation_events.parquet
signal_column: wick_reversal_proxy_event
parameters:
  lookback_window: 288
  z_threshold: 3.0
  cooldown_bars: 12
  merge_gap_bars: 0
  min_volume_quantile: 0.2
canonical_family: LIQUIDITY_DISLOCATION
trigger: Detector triggers when wick-dominance and reclaim structure align with abnormal realized volume.
confirmation: Follow-through should hold the reclaimed range while realized volume remains elevated versus baseline.
expected_behavior:
  holding_horizon_bars: 1-12
  invalidation_conditions:
  - Volume returns above median within 1-2 bars
  - Spread compresses back to baseline
  disabled_regimes:
  - 'SPREAD_BLOWOUT active: spread elevated, unreliable signal'
