# Proposal: btc_obs_2021_2022_campaign_h1_v1
# Generated by compiler agent from mechanism hypothesis btc_obs_2021_2022_campaign_h1_v1
# parent_hypothesis: none
# parent_run_id: btc_obs_2021_2022_campai_20260330T231926Z_rerun
#
# WARNING: horizon 72 is non-canonical for Path B (validate_hypothesis_spec / VALID_HORIZONS).
# It is fully supported on Path A (expand_hypotheses / parse_horizon_bars) which agent
# proposals use exclusively. If downstream code ever routes through Path B, 72b will be
# rejected. Do NOT substitute — the mechanism is frozen at 72 bars (~6 hours at 5m).

program_id: btc_obs_2021_2022_campaign
description: >
  VOL_SHOCK in elevated vol regime (vol_regime=high) drives leveraged long liquidations
  creating one-sided supply imbalance. Mean-reversion capital is risk-averse at high
  rv_percentile_24h (>=0.66), allowing downside continuation over 72 bars (~6h at 5m).
  Short direction only. Invalidate if 72b short t_stat < 1.5 with n >= 500.

run_mode: research
objective_name: retail_profitability
promotion_profile: disabled

symbols:
  - BTCUSDT

timeframe: 5m
start: "2021-01-01"
end: "2022-12-31"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - VOL_SHOCK
  canonical_regimes:
    - VOLATILITY_EXPANSION

templates:
  - continuation

horizons_bars:
  - 72

directions:
  - short

entry_lags:
  - 1
  - 2

contexts:
  vol_regime:
    - high

search_control: {}

discovery_profile: standard
phase2_gate_profile: auto
search_spec: spec/search_space.yaml
config_overlays: []
